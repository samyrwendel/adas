# Few-shots — exemplos canônicos da faixa `<nome>`

> Padrão do `spec-skills`. Coloque aqui 1–3 exemplos REAIS e preenchidos do "jeito certo" desta faixa
> (não placeholders): um arquivo por caso, nomeado `<caso>.example.<ext>`. A LLM imita o estilo da casa
> a partir destes exemplos, em vez de inventar do zero.

Ex.: `authenticated-endpoint.example.ts`, `card-default.example.tsx`, `error-message.example.ts`.

Cite cada few-shot relevante em `../mandatory-readings.md` pra a faixa apontar pra ele.
